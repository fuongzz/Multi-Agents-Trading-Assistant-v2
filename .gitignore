# ── Environment ──
.env
.env.*
!.env.example

# ── Python ──
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg
*.egg-info/
dist/
build/
.installed.cfg

# ── Virtual environments ──
.venv/
venv/
ENV/
env/

# ── Editable install (pyproject.toml) ──
*.dist-info/
*.egg-link

# ── Database (runtime, không commit) ──
*.db
*.sqlite
*.sqlite3

# ── Cache & output (runtime data) ──
multiagents_trading_assistant/cache/
multiagents_trading_assistant/output/
cache/
output/

# ── ChromaDB vector store ──
chroma_db/
multiagents_trading_assistant/cache/chromadb/

# ── Logs ──
*.log
logs/

# ── IDE ──
.vscode/
.idea/
*.suo
*.user

# ── OS ──
.DS_Store
Thumbs.db
desktop.ini

# ── Jupyter ──
.ipynb_checkpoints/
*.ipynb

# ── Streamlit ──
.streamlit/

# ── Test / Coverage ──
.coverage
htmlcov/
.pytest_cache/

# ── Backtest & report output (generated, không commit) ──
backtest_results/
reports/

# ── Runtime data ──
data/realtime_money_flow/
data/realtime_money_flow_test/
data/runtime/
data/samples/
data/research/macro_regime/
data/research/sector_rotation/
data/research/vnstock_gold_flow/
data/research/vnstock_gold_inventory/

# ── Temp / misc ──
tmp_*.txt
*.bak
*.bak2
*.bak3
*.bak4
.claude/scheduled_tasks.lock

# ── Trash (local archive, không public) ──
_trash/

# ── Build artifacts ──
ai_trading_assistant.egg-info/
*.egg-info/

# ── Notebooks (exploratory, không production) ──
notebooks/

# ── Local venv with non-standard name ──
UsersNC.venv/

# ════════════════════════════════════════════════════════════════
# Repo cleanup 2026-06-05 — chỉ giữ code khung/kiến trúc trên git
# (file vẫn còn ở local, chỉ ngừng theo dõi trên git)
# ════════════════════════════════════════════════════════════════

# ── Test suite & experiment: local-only ──
tests/

# ── scripts/: ignore tất cả, chỉ giữ lại script vận hành data pipeline ──
scripts/*
!scripts/auto_update_data_system.py
!scripts/build_index_master.py
!scripts/build_ohlcv_history.py
!scripts/enrich_ohlcv_metadata.py
!scripts/repair_ohlcv_bounds.py
!scripts/update_ohlcv_daily.py
!scripts/validate_data_quality.py

# ── Data sinh ra & artifact nghiên cứu ──
data/research/
*.parquet
multiagents_trading_assistant/data/strategy_memory.json
multiagents_trading_assistant/data/intraday/
multiagents_trading_assistant/edge_lab/evolved/
multiagents_trading_assistant/tradingagents_vn/memory/

# ── Launcher thử nghiệm ở root (giữ run.bat) ──
/run_*.bat
!/run.bat
/setup_*.ps1
/setup_*.bat
