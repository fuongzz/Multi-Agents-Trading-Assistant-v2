[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-trading-assistant"
version = "0.1.0"
requires-python = "==3.11.*"

[tool.setuptools.packages.find]
where = ["."]
include = ["multiagents_trading_assistant*"]
